// 全局 font-size 基准
$font-size = 14px
// 小字体,12px
$font-size-sm = 12px
// 对css变量定义一个引用
var-font-size = var(--font-size)
// 字体大小键值对
$fontSizeKV = { sm: $font-size-sm, lg: calc(var(--font-size) * 1.3), xl: calc(var(--font-size) * 1.6) }