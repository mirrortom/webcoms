// body font-size 基准,手机是16px
$font-size = 14px
// 如果没有必要,元素的字体大小不必设置,直接继承body
var-font-size = var(--font-size)
// html根元素
$rem-size = 100px
// 最小字体,12px
$font-size-sm = 12px
// 字体大小键值对
$fontSizeKV = { sm: $font-size-sm, lg: .16rem, xl: .2rem }