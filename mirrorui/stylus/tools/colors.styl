// 颜色,使用tailwind色卡

// 黑/白/灰
for colorName,list in $blackwhitegray-dict
    for saturation,val in list
        // 文字颜色
        .text-{colorName}-{saturation}
            color: val
        // 背景颜色
        .bg-{colorName}-{saturation}
            background-color: val
        // 边框颜色
        .bd-{colorName}-{saturation}
            border-color: val

// 彩色
for colorName,list in $colors-dict
    for saturation,val in list
        // 文字颜色
        .text-{colorName}-{saturation}
            color: val
        // 背景颜色
        .bg-{colorName}-{saturation}
            background-color: val
        // 边框颜色
        .bd-{colorName}-{saturation}
            border-color: val